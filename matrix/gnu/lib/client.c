/*
If GDB detects the program is multi-threaded, it augments
the usual message about stoping at breakpoint with the ID and name of thread
that hit the breakpoint.
*/
#ifdef client
#elif send_message
struct client
{
    /* data */
    __WINT_TYPE__ * __SIGNED_CHARS__ 
    continue;
};
#endif // client